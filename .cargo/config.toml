# Cargo build configuration
#
# Note: For local benchmarking with maximum performance, you can add:
#   rustflags = ["-C", "target-cpu=native"]
# to a [build] section, but this makes binaries non-portable.

[profile.release]
# Disable incremental compilation for reproducible builds
incremental = false
